import GameLayout from "@/components/GameLayout"; // Importuj nowy komponent

const Index = () => {
  return (
    // Renderuj komponent GameLayout zamiast domyślnego tekstu
    <GameLayout />
  );
};

export default Index;